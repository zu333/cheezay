{
  "rules": {
    ".read": true,
    ".write": false,
    "siteSettings": {
      "heroImage": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['data','updatedAt']) && newData.child('data').isString()"
      }
    }
  }
}
