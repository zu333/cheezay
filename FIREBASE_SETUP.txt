CHEEZAY DIKHAO - FIREBASE SETUP

1. Firebase Console > Authentication > Sign-in method:
   - Enable Google.
   - Enable Anonymous (required for the hidden admin password flow).

2. Realtime Database:
   - Import/apply database.rules.json.
   - The current rule allows authenticated users to write. Because the admin password is stored in frontend code, this is a convenience/demo admin gate, NOT strong security.

3. Hidden Admin:
   - Footer Admin Settings link has been removed.
   - Cheezay Dikhao logo ko 5 dafa jaldi click karne par Admin Settings open hoti hain.
   - Sirf password: nabel3457
   - Admin mein device se hero image upload, resize/crop, preview, Firebase save/reload available hai.

4. Normal Login:
   - Login button mein Google login active code added hai.
   - Google provider Firebase Console mein enable karna zaroori hai.
   - Email/password login bhi Firebase Auth se connected hai.

5. Vercel/GitHub:
   - index.html, cropped-logo.jpg, default-hero.jpg aur database.rules.json ko same project root mein rakhein.
   - Build command: empty
   - Output directory: .
